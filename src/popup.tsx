import React from 'react';
import * as ReactDOM from 'react-dom';
import { ExtensionPopup } from './components/environments/ExtensionPopup';

ReactDOM.render(<ExtensionPopup />, document.getElementById('app'));
